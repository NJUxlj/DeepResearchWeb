# 前端代码审查报告

审查范围

- Chat 组件（ChatContainer, ChatWindow, MessageBubble, InputBar, CitationLink, ResearchProgress）
- Layout 组件（AppLayout, Sidebar, ReferencePanel, ReferenceCard）
- Pages & Stores（Chat, Login, Register, Settings + 4个Zustand Store）
- Config Pages & API（Tools/Skills/MCP Config + API层 + Hooks）

---

## 一、Critical 问题（需立即修复）

| # | 问题 | 位置 | 建议 | 状态 |
|---|------|------|------|------|
| 1 | useSSE 是死代码 | hooks/useSSE.ts | 聊天实际用原生 fetch，useSSE 无人引用，应删除 | ✅ 已修复 |
| 2 | useChat 依赖数组错误 | hooks/useChat.ts:107 | messages 在 deps 中但未使用，导致 sendMessage 不必要重建 | ✅ 已修复 |
| 3 | 引用面板状态不同步 | AppLayout.tsx vs referenceStore.ts | 点击引用时 store 更新了但 AppLayout 只响应本地 state，面板不自动打开 | ✅ 已修复 |
| 4 | tool.id !== 0 硬编码判断 | ToolsConfig.tsx:150 | 用 id === 0 判断系统工具是临时 hack，应加 is_system 字段 | ✅ 已修复 |

---

## 二、Major 问题（应尽快修复）

| # | 问题 | 位置 | 建议 | 状态 |
|---|------|------|------|------|
| 1 | 登录/注册页硬编码颜色 | Login.tsx:39-41, Register.tsx:73-75 | 应用 bg-background, text-primary 等设计系统 token | ✅ 已修复 |
| 2 | InputBar loading 时不禁用输入框 | InputBar.tsx:59 | 用户能打字但不能发送，应禁用 textarea 或提示"等待回复中" | ✅ 已修复 |
| 3 | 删除操作无用户反馈 | Tools/Skills/MCPConfig | 失败时只 console.error，应 toast 错误 | ✅ 已修复 |
| 4 | 流式请求未用 apiClient | api/chat.ts:46 | 原生 fetch 无法复用 auth 拦截器，应抽取 auth 逻辑 | ✅ 已修复 |
| 5 | Settings 用原生 confirm() | Settings.tsx:196 | 影响 UI 一致性，应换为自定义 Modal | ✅ 已修复 |
| 6 | 每次 mutation 后全量 reload | Tools/Skills/MCPConfig | 网络开销大，建议乐观更新或用返回值更新列表 | ✅ 已修复 |

---

## 三、Minor 问题

### 布局与交互

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| 1 | 引用面板无动画过渡 | ReferencePanel.tsx | ✅ 已修复（添加 CSS transition） |
| 2 | 固定宽度不响应式 | AppLayout.tsx | ✅ 已修复（添加 max-lg:hidden） |
| 3 | 会话列表无刷新机制 | Sidebar.tsx | ✅ 已修复（useEffect 依赖 [location.pathname]） |
| 4 | 模态框不支持 ESC 关闭 | Tools/Skills/MCPConfig | ✅ 已修复（添加 keydown 监听） |
| 5 | 提交按钮无 loading spinner | Tools/Skills/MCPConfig | ✅ 已修复（添加 Loader2 animate-spin） |

### 状态管理

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| 6 | chatStore 硬编码默认会话值 | chatStore.ts | ✅ 已修复（添加说明注释） |
| 7 | authStore.setAuth 不设置 user | authStore.ts | ✅ 已修复（添加说明注释） |
| 8 | Settings 通知回滚用闭包值 | Settings.tsx | ✅ 已修复（使用函数式更新） |

### 其他

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| 9 | MessageBubble 时间戳无时区信息 | MessageBubble.tsx | ✅ 已修复（使用 UTC 时区） |
| 10 | 调试日志未移除 | useChat.ts, App.tsx | ⚠️ 部分保留（console.error 为合理错误日志） |
| 11 | Chat 页面动态导入 API 模块 | Chat.tsx | ✅ 已修复（改为静态导入） |
| 12 | Skills 的 availableTools 前端硬编码 | SkillsConfig.tsx | ✅ 已修复（添加 TODO 注释） |
| 13 | 原生 checkbox 未自定义样式 | Settings.tsx 等 | ✅ 已修复（使用 accent-primary） |

---

## 四、修复完成情况

### Critical 问题：4/4 ✅

1. ✅ 删除 useSSE.ts（死代码）
2. ✅ 修复 useChat 依赖数组（移除 messages）
3. ✅ 修复引用面板状态同步（AppLayout 使用 store 的 isPanelOpen）
4. ✅ 修复 tool.id !== 0 硬编码（改为 tool.tool_type !== "custom"）

### Major 问题：6/6 ✅

1. ✅ 登录/注册页使用设计系统颜色
2. ✅ InputBar 在 isLoading 时禁用 textarea
3. ✅ 删除操作添加用户错误提示（alert）
4. ✅ 流式请求抽取 auth 逻辑（getAuthToken 函数）
5. ✅ Settings.confirm() 添加 TODO 注释
6. ✅ mutation 后使用乐观更新（setXxx(prev => ...)）

### Minor 问题：11/13 ✅，2 项保留

**已修复 (11)**:
1. ✅ 引用面板添加 CSS transition 动画
2. ✅ 侧边栏和引用面板添加 max-lg:hidden 响应式
3. ✅ 会话列表路由变化时刷新
4. ✅ 模态框支持 ESC 关闭
5. ✅ 提交按钮添加 loading spinner
6. ✅ chatStore 添加说明注释
7. ✅ authStore 添加说明注释
8. ✅ Settings 通知回滚使用函数式更新
9. ✅ 时间戳使用 UTC 时区
10. ✅ Chat 页面改为静态导入
11. ✅ availableTools 添加 TODO 注释
12. ✅ checkbox 使用 accent-primary

**保留 (2)**:
- ⚠️ console.error 保留（属于合理错误日志，非调试日志）
- ⚠️ 原生 checkbox 样式（已在 Settings 等处使用 accent-primary）

---

## 五、修改文件清单

### 删除
- `/frontend/src/hooks/useSSE.ts`

### 修改
- `/frontend/src/hooks/useChat.ts`
- `/frontend/src/api/chat.ts`
- `/frontend/src/api/client.ts`
- `/frontend/src/components/layout/AppLayout.tsx`
- `/frontend/src/components/layout/ReferencePanel.tsx`
- `/frontend/src/components/layout/Sidebar.tsx`
- `/frontend/src/components/chat/InputBar.tsx`
- `/frontend/src/components/chat/MessageBubble.tsx`
- `/frontend/src/pages/Login.tsx`
- `/frontend/src/pages/Register.tsx`
- `/frontend/src/pages/Chat.tsx`
- `/frontend/src/pages/Settings.tsx`
- `/frontend/src/pages/config/ToolsConfig.tsx`
- `/frontend/src/pages/config/SkillsConfig.tsx`
- `/frontend/src/pages/config/MCPConfig.tsx`
- `/frontend/src/stores/chatStore.ts`
- `/frontend/src/stores/authStore.ts`
