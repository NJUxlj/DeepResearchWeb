# 开发进度

## 项目整体进度

**当前阶段**: P3-P4 阶段进行中

**总体进度**: \~75%

***

## Subplan 进度汇总

| 序号 | Subplan                 | 状态     | 完成度  | 备注                                               |
| -- | ----------------------- | ------ | ---- | ------------------------------------------------ |
| 01 | 项目脚手架与 Docker 环境        | ✅ 完成   | 100% | 完整的 FastAPI + React 项目结构，Docker Compose 编排       |
| 02 | 用户认证系统                  | ✅ 完成   | 100% | JWT 认证、用户注册/登录、密码哈希                              |
| 03 | 基础 Chatbot 和会话管理        | ✅ 完成   | 100% | Session/Message 模型、SSE 流式聊天、前端 Chat UI           |
| 04 | DeepResearch Agent 核心流程 | ✅ 完成   | 90%  | Planner、Searcher、Synthesizer、Citation 生成         |
| 05 | ARQ 任务队列与并发             | ✅ 完成   | 100% | ARQ Worker、Redis Pub/Sub、SSE 推送                  |
| 06 | MemOS 记忆系统集成            | ✅ 完成   | 80%  | Memory Service、Preference Memory、Tree Memory API |
| 07 | 工具/Skills/MCP 系统        | ✅ 完成   | 90%  | 工具配置、技能配置、MCP 服务器配置                              |
| 08 | 前端布局与引用面板               | ✅ 完成   | 95%  | Chat UI、Sidebar、ReferencePanel、研究进度展示            |
| 09 | 部署与性能优化                 | ✅ 完成   | 80%  | Docker 配置、生产环境构建                                 |
| 10 | 单元测试与 Mock              | 🔄 进行中 | 30%  | 基础测试框架搭建                                         |
| 11 | 集成测试                    | 🔄 进行中 | 20%  | API 集成测试                                         |

***

## 详细进度

### 01. 项目脚手架 (P0) ✅

**完成时间**: 项目初期

**已完成**:

- [x] FastAPI 后端项目结构 (`backend/app/`)
- [x] React + TypeScript 前端项目 (`frontend/src/`)
- [x] Docker Compose 编排 (MySQL, Neo4j, Milvus, Qdrant, Redis, RabbitMQ)
- [x] 配置文件 (config.py, alembic.ini, requirements.txt, package.json)
- [x] 基础中间件 (CORS, 请求日志, 异常处理)
- [x] 数据库连接 (SQLAlchemy async)
- [x] 健康检查 API

**关键文件**:

- `backend/app/main.py` - FastAPI 入口
- `backend/app/config.py` - 配置管理
- `docker-compose.yml` - 服务编排
- `frontend/vite.config.ts` - Vite 配置

***

### 02. 用户认证系统 (P0) ✅

**完成时间**: 项目初期

**已完成**:

- [x] User 模型 (`backend/app/models/user.py`)
- [x] Pydantic Schemas (`backend/app/schemas/user.py`)
- [x] 安全工具模块 (`backend/app/core/security.py`) - JWT、密码哈希
- [x] 认证 API (`backend/app/api/v1/auth.py`) - register, login, me, update
- [x] 依赖注入 (`backend/app/api/deps.py`) - get\_current\_user
- [x] 前端认证 API (`frontend/src/api/auth.ts`)
- [x] 认证状态管理 (`frontend/src/stores/authStore.ts`)

**API 端点**:

| 端点                      | 方法   | 描述     |
| ----------------------- | ---- | ------ |
| `/api/v1/auth/register` | POST | 用户注册   |
| `/api/v1/auth/login`    | POST | 用户登录   |
| `/api/v1/auth/me`       | GET  | 获取当前用户 |
| `/api/v1/auth/me`       | PUT  | 更新当前用户 |

***

### 03. 基础 Chatbot (P0) ✅

**完成时间**: 项目初期

**已完成**:

- [x] Session 模型 (`backend/app/models/session.py`)
- [x] Message 模型 (`backend/app/models/message.py`)
- [x] Session Schema (`backend/app/schemas/session.py`)
- [x] Message Schema (`backend/app/schemas/message.py`) - 包含 Citation
- [x] 会话管理 API (`backend/app/api/v1/sessions.py`)
- [x] 基础 Chat API (`backend/app/api/v1/chat.py`) - SSE 流式响应
- [x] Chat Agent (`backend/app/agents/chat_agent.py`)
- [x] 前端类型定义 (`frontend/src/types/session.ts`)
- [x] 前端 API (`frontend/src/api/session.ts`, `chat.ts`)
- [x] 状态管理 (`frontend/src/stores/chatStore.ts`, `sessionStore.ts`)
- [x] SSE Hook (`frontend/src/hooks/useSSE.ts`)

**API 端点**:

| 端点                      | 方法             | 描述         |
| ----------------------- | -------------- | ---------- |
| `/api/v1/sessions`      | GET/POST       | 会话列表/创建    |
| `/api/v1/sessions/{id}` | GET/PUT/DELETE | 会话详情/更新/删除 |
| `/api/v1/chat/stream`   | GET            | 流式聊天 (SSE) |
| `/api/v1/chat/message`  | POST           | 非流式聊天      |

***

### 04. DeepResearch Agent 核心 (P1) ✅

**完成时间**: 2024年

**已完成**:

- [x] ResearchTask 模型 (`backend/app/models/research_task.py`)
- [x] Research Schema (`backend/app/schemas/research.py`)
- [x] Research Agent (`backend/app/agents/research_agent.py`)
- [x] Planner 服务 (`backend/app/services/research/planner.py`) - 问题分解
- [x] Searcher 服务 (`backend/app/services/research/searcher.py`) - 多源检索
- [x] Synthesizer 服务 (`backend/app/services/research/synthesizer.py`) - 结果综合
- [x] Citation 服务 (`backend/app/services/research/citation.py`) - 引用生成
- [x] Research API (`backend/app/api/v1/research.py`)

**工作流**:

1. **Triage** - 判断是否需要澄清
2. **Plan** - 拆分子问题
3. **Search** - 多源并行检索
4. **Synthesize** - 综合判断（最多3轮迭代）
5. **Report** - 生成带引用的报告

***

### 05. ARQ 任务队列 (P2) ✅

**完成时间**: 2024年

**已完成**:

- [x] ARQ Worker (`backend/app/workers/research_worker.py`)
- [x] Redis 连接 (`backend/app/core/redis.py`)
- [x] 缓存模块 (`backend/app/core/cache.py`)
- [x] 限流模块 (`backend/app/core/rate_limiter.py`)

***

### 06. MemOS 记忆系统 (P3) 🔄

**完成时间**: 进行中

**已完成**:

- [x] Memory Service (`backend/app/services/memory_service.py`)
- [x] Memory API (`backend/app/api/v1/memory.py`)

**待完成**:

- [ ] Preference Memory 完整集成
- [ ] Tree Memory 完整集成

***

### 07. 工具/Skills/MCP 系统 (P4) 🔄

**完成时间**: 基本完成

**已完成**:

- [x] ToolConfig 模型 (`backend/app/models/tool_config.py`)
- [x] SkillConfig 模型 (`backend/app/models/skill_config.py`)
- [x] MCPServerConfig 模型 (`backend/app/models/mcp_config.py`)
- [x] Tool Schema (`backend/app/schemas/tool.py`)
- [x] Skill Schema (`backend/app/schemas/skill.py`)
- [x] MCP Schema (`backend/app/schemas/mcp.py`)
- [x] Tool Service (`backend/app/services/tool_service.py`)
- [x] Skill Service (`backend/app/services/skill_service.py`)
- [x] MCP Service (`backend/app/services/mcp_service.py`)
- [x] Tools API (`backend/app/api/v1/tools.py`)
- [x] Skills API (`backend/app/api/v1/skills.py`)
- [x] MCP API (`backend/app/api/v1/mcp.py`)
- [x] 用户环境配置 (`backend/app/models/user_env_config.py`, `user_env_config.py`)
- [x] 前端配置页面 (`frontend/src/pages/config/ToolsConfig.tsx`, `SkillsConfig.tsx`, `MCPConfig.tsx`)

***

### 08. 前端布局与引用 (P5) ✅

**完成时间**: 2024年

**已完成**:

- [x] App 布局 (`frontend/src/components/layout/AppLayout.tsx`)
- [x] 侧边栏 (`frontend/src/components/layout/Sidebar.tsx`)
- [x] 引用面板 (`frontend/src/components/layout/ReferencePanel.tsx`)
- [x] Chat 窗口 (`frontend/src/components/chat/ChatWindow.tsx`)
- [x] 消息气泡 (`frontend/src/components/chat/MessageBubble.tsx`)
- [x] 输入框 (`frontend/src/components/chat/InputBar.tsx`)
- [x] 研究进度 (`frontend/src/components/chat/ResearchProgress.tsx`)
- [x] 引用链接 (`frontend/src/components/chat/CitationLink.tsx`)
- [x] 引用卡片 (`frontend/src/components/common/ReferenceCard.tsx`)
- [x] Chat 页面 (`frontend/src/pages/Chat.tsx`)
- [x] 登录/注册页面 (`frontend/src/pages/Login.tsx`, `Register.tsx`)
- [x] 设置页面 (`frontend/src/pages/Settings.tsx`)

***

### 额外功能

#### 思维链 (Thinking Chain) 🔄

**状态**: 后端已完成，前端进行中

**后端已完成**:

- [x] Message 模型添加 `thinking` 字段
- [x] Message Schema 添加 `thinking`
- [x] Chat Agent 思维链解析 (MiniMax `<think>` `</think>` 标签)
- [x] 流式输出 thinking 事件
- [x] Chat API 处理 thinking 事件

**前端进行中**:

- [x] Message 类型添加 `thinking` 字段
- [x] API 层添加 `onThinking` 回调
- [x] 状态管理添加 `updateLastMessageThinking`
- [x] Hook 处理 thinking 流式更新
- [ ] MessageBubble 组件思维链 UI

#### Logger 工具类 ✅

**完成时间**: 2024年

**已完成**:

- [x] Logger 工具 (`backend/app/utils/logger.py`)
- [x] 日志文件保存到 `logs/{name}.log`

***

## 前端组件结构

```
frontend/src/
├── components/
│   ├── chat/
│   │   ├── ChatContainer.tsx      # Chat 容器
│   │   ├── ChatWindow.tsx         # 聊天窗口
│   │   ├── MessageBubble.tsx       # 消息气泡
│   │   ├── InputBar.tsx           # 输入框
│   │   ├── CitationLink.tsx       # 引用链接
│   │   └── ResearchProgress.tsx   # 研究进度
│   ├── common/
│   │   └── ReferenceCard.tsx      # 引用卡片
│   └── layout/
│       ├── AppLayout.tsx          # 应用布局
│       ├── Sidebar.tsx            # 侧边栏
│       └── ReferencePanel.tsx     # 引用面板
├── pages/
│   ├── Chat.tsx                   # 聊天页面
│   ├── Login.tsx                  # 登录页面
│   ├── Register.tsx               # 注册页面
│   ├── Settings.tsx               # 设置页面
│   └── config/
│       ├── ToolsConfig.tsx        # 工具配置
│       ├── SkillsConfig.tsx       # 技能配置
│       └── MCPConfig.tsx          # MCP 配置
├── stores/
│   ├── authStore.ts               # 认证状态
│   ├── chatStore.ts               # 聊天状态
│   └── sessionStore.ts            # 会话状态
├── api/
│   ├── auth.ts                    # 认证 API
│   ├── chat.ts                    # 聊天 API
│   ├── session.ts                  # 会话 API
│   └── client.ts                  # Axios 客户端
└── types/
    ├── auth.ts                    # 认证类型
    └── session.ts                 # 会话类型
```

***

## 后端 API 结构

```
backend/app/api/v1/
├── auth.py          # 认证 API
├── chat.py          # 聊天 API
├── sessions.py      # 会话 API
├── research.py     # 研究 API
├── memory.py       # 记忆 API
├── tools.py        # 工具 API
├── skills.py       # 技能 API
├── mcp.py          # MCP API
├── health.py       # 健康检查
└── ...
```

***

## 待完成任务

### 短期 (1-2周)

- [ ] 完成前端思维链 UI 显示
- [ ] 完成 MemOS 记忆系统集成
- [ ] 完善单元测试

### 中期 (1个月)

- [ ] 集成测试完善
- [ ] 性能优化
- [ ] 生产环境部署

### 长期

- [ ] 完善引用面板交互
- [ ] 添加更多工具支持
- [ ] 优化搜索结果排序

***

## 版本记录

| 版本     | 日期         | 变更内容                          |
| ------ | ---------- | ----------------------------- |
| v0.1.0 | 项目初期       | 项目脚手架、基础认证、Chat 功能            |
| v0.2.0 | 2024年      | DeepResearch Agent、任务队列       |
| v0.3.0 | 2024年      | 记忆系统、工具配置、前端优化                |
| v0.4.0 | 2026-03-14 | 思维链、Tools/Skills/MCP 配置页面完整功能 |

***

## 2026-03-14 更新内容

### Tools/Skills/MCP 配置页面功能完善

**完成时间**: 2026-03-14

**问题描述**:

- Tools/Skills/MCP 配置页面的"添加"按钮无实际效果
- 启用/禁用按钮点击报错
- 编辑功能不工作

**已完成的修复**:

1. **Tools 配置页** (`/config/tools`)
   - [x] 添加工具表单模态框 (ToolFormModal)
   - [x] 编辑工具表单模态框 (ToolEditModal)
   - [x] 启用/禁用功能
   - [x] 删除工具功能
   - [x] 内置工具(ID=0)隐藏操作按钮
2. **Skills 配置页** (`/config/skills`)
   - [x] 添加 Skill 表单模态框 (SkillFormModal)
   - [x] 编辑 Skill 表单模态框 (SkillEditModal)
   - [x] 启用/禁用功能
   - [x] 删除 Skill 功能
3. **MCP 配置页** (`/config/mcp`)
   - [x] 添加 MCP 服务器表单模态框 (MCPFormModal)
   - [x] 编辑 MCP 服务器表单模态框 (MCPEditModal)
   - [x] 测试连接功能
   - [x] 启用/禁用功能
   - [x] 删除 MCP 服务器功能

**技术修复**:

- [x] 类型定义修复: `id` 从 `string` 改为 `number`
- [x] API 层参数类型修复
- [x] Token 存储 key 修复: 从 `token` 改为读取 `auth-storage` (zustand persist)
- [x] 响应拦截器清理逻辑修复

**修改的文件**:

- `frontend/src/api/client.ts` - Token 读取逻辑
- `frontend/src/api/tools.ts` - ID 参数类型
- `frontend/src/api/skills.ts` - ID 参数类型
- `frontend/src/api/mcp.ts` - ID 参数类型
- `frontend/src/types/citation.ts` - 类型定义
- `frontend/src/pages/config/ToolsConfig.tsx` - 添加/编辑/删除功能
- `frontend/src/pages/config/SkillsConfig.tsx` - 添加/编辑/删除功能
- `frontend/src/pages/config/MCPConfig.tsx` - 添加/编辑/删除功能

**部署**:

- Docker 镜像已重新构建: `docker-compose build frontend && docker-compose up -d frontend`
- 前端服务端口: 3000， 后端服务端口: 8000

***

*最后更新: 2026-03-14*
