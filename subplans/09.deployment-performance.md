# 09. 部署配置与性能优化

## 目标

完成部署配置和性能优化：
- Docker Compose 生产环境配置
- Nginx 反向代理配置
- 环境变量配置模板
- 性能调优参数
- 监控与健康检查

## 依赖

所有前序 subplan

## 被依赖

无（最后阶段）

---

## 1. Docker Compose 生产配置

### `docker-compose.prod.yml`

```yaml
version: "3.9"

services:
  # ========== 数据存储层 ==========
  
  mysql:
    image: mysql:8.0
    container_name: drw_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: deepresearch
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./scripts/mysql/init:/docker-entrypoint-initdb.d
    ports:
      - "127.0.0.1:3306:3306"
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --max_connections=200
      --innodb_buffer_pool_size=1G
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - drw_network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  neo4j:
    image: neo4j:5-community
    container_name: drw_neo4j
    restart: always
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD}
      NEO4J_PLUGINS: '["apoc", "gds"]'
      NEO4J_dbms_memory_heap_initial__size: 1G
      NEO4J_dbms_memory_heap_max__size: 2G
      NEO4J_dbms_memory_pagecache_size: 1G
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    ports:
      - "127.0.0.1:7474:7474"
      - "127.0.0.1:7687:7687"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:7474"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - drw_network
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G

  milvus-etcd:
    image: quay.io/coreos/etcd:v3.5.5
    container_name: drw_milvus_etcd
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
    volumes:
      - milvus_etcd:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    networks:
      - drw_network

  milvus-minio:
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    container_name: drw_milvus_minio
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    volumes:
      - milvus_minio:/minio_data
    command: minio server /minio_data --console-address ":9001"
    networks:
      - drw_network

  milvus-standalone:
    image: milvusdb/milvus:v2.4.1
    container_name: drw_milvus
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: milvus-etcd:2379
      MINIO_ADDRESS: milvus-minio:9000
    volumes:
      - milvus_data:/var/lib/milvus
    ports:
      - "127.0.0.1:19530:19530"
    depends_on:
      - milvus-etcd
      - milvus-minio
    networks:
      - drw_network
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G

  qdrant:
    image: qdrant/qdrant:v1.15.3
    container_name: drw_qdrant
    restart: always
    volumes:
      - qdrant_data:/qdrant/storage
    ports:
      - "127.0.0.1:6333:6333"
      - "127.0.0.1:6334:6334"
    environment:
      QDRANT__SERVICE__GRPC_PORT: 6334
      QDRANT__SERVICE__HTTP_PORT: 6333
    networks:
      - drw_network
    deploy:
      resources:
        limits:
          memory: 2G

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: drw_rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    ports:
      - "127.0.0.1:5672:5672"
      - "127.0.0.1:15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - drw_network

  redis:
    image: redis:7-alpine
    container_name: drw_redis
    restart: always
    volumes:
      - redis_data:/data
    ports:
      - "127.0.0.1:6379:6379"
    command: >
      redis-server
      --maxmemory 1gb
      --maxmemory-policy allkeys-lru
      --appendonly yes
      --appendfsync everysec
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - drw_network
    deploy:
      resources:
        limits:
          memory: 1G

  # ========== 应用层 ==========

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: drw_backend
    restart: always
    environment:
      - DATABASE_URL=mysql+pymysql://${MYSQL_USER}:${MYSQL_PASSWORD}@mysql:3306/deepresearch
      - REDIS_URL=redis://redis:6379/0
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
      - MILVUS_HOST=milvus-standalone
      - MILVUS_PORT=19530
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333
      - RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@rabbitmq:5672/
      - DEBUG=false
      - ENV=production
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      milvus-standalone:
        condition: service_started
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
    command: >
      gunicorn app.main:app
      -w 4
      -k uvicorn.workers.UvicornWorker
      --bind 0.0.0.0:8000
      --access-logfile -
      --error-logfile -
      --access-logformat '%(h)s %(l)s %(u)s %(t)s "%(r)s" %(s)s %(b)s "%(f)s" "%(a)s" %(D)s'
    networks:
      - drw_network
    deploy:
      replicas: 2
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    environment:
      - DATABASE_URL=mysql+pymysql://${MYSQL_USER}:${MYSQL_PASSWORD}@mysql:3306/deepresearch
      - REDIS_URL=redis://redis:6379/0
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
      - MILVUS_HOST=milvus-standalone
      - MILVUS_PORT=19530
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333
      - RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@rabbitmq:5672/
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      neo4j:
        condition: service_healthy
    command: arq app.workers.research_worker.WorkerSettings
    networks:
      - drw_network
    deploy:
      replicas: 2
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: drw_frontend
    restart: always
    depends_on:
      - backend
    networks:
      - drw_network
    deploy:
      resources:
        limits:
          memory: 256M

  nginx:
    image: nginx:alpine
    container_name: drw_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - backend
      - frontend
    networks:
      - drw_network

volumes:
  mysql_data:
  neo4j_data:
  neo4j_logs:
  milvus_etcd:
  milvus_minio:
  milvus_data:
  qdrant_data:
  rabbitmq_data:
  redis_data:

networks:
  drw_network:
    driver: bridge
```

---

## 2. Nginx 配置

### `nginx/nginx.conf`

```nginx
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # 日志格式
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for" '
                    'rt=$request_time uct="$upstream_connect_time" '
                    'uht="$upstream_header_time" urt="$upstream_response_time"';

    access_log /var/log/nginx/access.log main;

    # 性能优化
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml;

    # 上游服务器
    upstream backend {
        least_conn;
        server backend:8000 max_fails=3 fail_timeout=30s;
    }

    upstream frontend {
        server frontend:80;
    }

    # HTTP 重定向到 HTTPS
    server {
        listen 80;
        server_name _;
        return 301 https://$host$request_uri;
    }

    # HTTPS 服务器
    server {
        listen 443 ssl http2;
        server_name localhost;

        # SSL 配置
        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;

        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # 前端静态资源
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # API 代理
        location /api/ {
            proxy_pass http://backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # SSE 支持
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 86400s;
            proxy_send_timeout 86400s;
        }

        # 健康检查
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}
```

---

## 3. 环境变量模板

### `.env.example`

```bash
# ============================================
# DeepResearchWeb 环境变量配置模板
# ============================================
# 复制此文件为 .env 并填写实际值

# ========== 数据库配置 ==========
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_USER=drw
MYSQL_PASSWORD=change_me_db_password

# ========== Neo4j 配置 ==========
NEO4J_PASSWORD=change_me_neo4j_password

# ========== RabbitMQ 配置 ==========
RABBITMQ_USER=drw
RABBITMQ_PASSWORD=change_me_rabbit_password

# ========== JWT 密钥 ==========
# 生成命令: openssl rand -hex 32
SECRET_KEY=change_me_32_char_secret_key_minimum_length

# ========== LLM 配置 ==========
OPENAI_API_KEY=sk-your_openai_api_key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# ========== 搜索 API 密钥 ==========
TAVILY_API_KEY=tvly-your_tavily_key
SERPAPI_KEY=your_serpapi_key

# ========== 可选配置 ==========
# DEBUG=false
# ENV=production
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# LLM_MAX_CONCURRENT=20
```

---

## 4. 性能调优参数汇总

### 数据库连接池

| 服务 | 参数 | 值 | 说明 |
|------|------|-----|------|
| MySQL | pool_size | 20 | 基础连接数 |
| MySQL | max_overflow | 30 | 最大溢出连接 |
| Neo4j | max_connection_pool_size | 50 | 连接池大小 |
| Milvus | max_connections | 30 | 最大连接数 |
| Redis | max_connections | 100 | 连接池大小 |

### 并发控制

| 组件 | 参数 | 值 | 说明 |
|------|------|-----|------|
| API Workers | gunicorn workers | 4 | Uvicorn Worker 数 |
| ARQ Workers | max_jobs | 50 | 最大并发任务 |
| LLM 调用 | semaphore | 20 | 并发 LLM 调用限制 |
| MCP 进程 | max_processes | 20 | 最大 MCP 进程数 |

### 缓存配置

| 类型 | TTL | 说明 |
|------|-----|------|
| 搜索结果 | 1800s | 30 分钟 |
| LLM 响应 | 3600s | 1 小时（可选） |

---

## 5. 监控与健康检查

### `backend/app/api/v1/monitoring.py`

```python
"""监控和健康检查端点."""

from fastapi import APIRouter, Depends
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.deps import get_db
from app.core.redis import get_redis

router = APIRouter(prefix="/monitoring", tags=["monitoring"])


@router.get("/health")
async def health_check(db: AsyncSession = Depends(get_db)):
    """健康检查端点."""
    checks = {
        "database": "unknown",
        "redis": "unknown",
    }
    status = "healthy"

    # 检查数据库
    try:
        await db.execute(text("SELECT 1"))
        checks["database"] = "connected"
    except Exception as e:
        checks["database"] = f"error: {e}"
        status = "unhealthy"

    # 检查 Redis
    try:
        redis = await get_redis()
        await redis.ping()
        checks["redis"] = "connected"
    except Exception as e:
        checks["redis"] = f"error: {e}"
        status = "unhealthy"

    return {
        "status": status,
        "checks": checks,
    }


@router.get("/metrics")
async def metrics():
    """基础指标端点（简化版）."""
    import psutil
    import os

    # 系统资源
    memory = psutil.virtual_memory()
    cpu_percent = psutil.cpu_percent(interval=1)

    return {
        "system": {
            "cpu_percent": cpu_percent,
            "memory_used_mb": memory.used / 1024 / 1024,
            "memory_total_mb": memory.total / 1024 / 1024,
            "memory_percent": memory.percent,
        },
        "process": {
            "pid": os.getpid(),
        },
    }
```

---

## 6. 部署脚本

### `scripts/deploy.sh`

```bash
#!/bin/bash
set -e

echo "=== DeepResearchWeb 部署脚本 ==="

# 检查环境变量文件
if [ ! -f .env ]; then
    echo "错误: .env 文件不存在，请从 .env.example 复制并配置"
    exit 1
fi

# 创建必要目录
mkdir -p nginx/ssl
mkdir -p logs

# 生成 SSL 证书（如果没有）
if [ ! -f nginx/ssl/cert.pem ]; then
    echo "生成自签名 SSL 证书..."
    openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
        -keyout nginx/ssl/key.pem \
        -out nginx/ssl/cert.pem \
        -subj "/C=US/ST=State/L=City/O=Organization/CN=localhost"
fi

# 拉取最新镜像
echo "拉取 Docker 镜像..."
docker-compose -f docker-compose.prod.yml pull

# 启动服务
echo "启动服务..."
docker-compose -f docker-compose.prod.yml up -d

# 执行数据库迁移
echo "执行数据库迁移..."
docker-compose -f docker-compose.prod.yml exec backend alembic upgrade head

# 健康检查
echo "等待服务启动..."
sleep 10

if curl -sf http://localhost/api/v1/health > /dev/null; then
    echo "✅ 部署成功！"
else
    echo "⚠️ 服务可能未完全启动，请检查日志"
    docker-compose -f docker-compose.prod.yml logs backend
fi

echo ""
echo "服务访问地址:"
echo "  - Web UI: https://localhost"
echo "  - API Docs: https://localhost/docs (开发模式)"
echo "  - RabbitMQ Management: http://localhost:15672"
```

---

## 验证清单

- [ ] Docker Compose 生产配置可正常启动
- [ ] Nginx 反向代理配置正确
- [ ] SSL 证书配置正确
- [ ] 环境变量模板完整
- [ ] 健康检查端点工作正常
- [ ] 部署脚本可正常执行
- [ ] 各服务资源限制配置合理
- [ ] 日志配置正确

---

## 7. 监控与可观测性配置

### Prometheus 配置

#### `monitoring/prometheus.yml`

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'backend'
    static_configs:
      -:8000']
 targets: ['backend    metrics_path: '/api/v1/metrics'

  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']

  - job_name: 'mysql'
    static_configs:
      - targets: ['mysql-exporter:9104']

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
```

#### `docker-compose.monitoring.yml`

```yaml
version: "3.9"

services:
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: drw_prometheus
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    ports:
      - "9090:9090"
    networks:
      - drw_network

  grafana:
    image: grafana/grafana:10.2.2
    container_name: drw_grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin123}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources
    ports:
      - "3001:3000"
    networks:
      - drw_network

  node-exporter:
    image: prom/node-exporter:v1.6.1
    container_name: drw_node_exporter
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    ports:
      - "9100:9100"
    networks:
      - drw_network

  redis-exporter:
    image: oliver006/redis_exporter:v1.54.0
    container_name: drw_redis_exporter
    environment:
      REDIS_ADDR: redis://redis:6379
    ports:
      - "9121:9121"
    networks:
      - drw_network

  mysql-exporter:
    image: prom/mysqld-exporter:v0.16.0
    container_name: drw_mysql_exporter
    environment:
      DATA_SOURCE_NAME: "drw:${MYSQL_PASSWORD}@(mysql:3306)/"
    ports:
      - "9104:9104"
    networks:
      - drw_network

volumes:
  prometheus_data:
  grafana_data:

networks:
  drw_network:
    external: true
```

### Grafana 仪表板配置

#### `monitoring/grafana/datasources/datasources.yml`

```yaml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
```

### 后端指标端点扩展

```python
"""监控和健康检查端点 - 扩展版."""

from fastapi import APIRouter, Depends
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.deps import get_db
from app.core.redis import get_redis
import psutil
import os

router = APIRouter(prefix="/monitoring", tags=["monitoring"])


@router.get("/metrics")
async def metrics():
    """基础指标端点."""
    # 系统资源
    memory = psutil.virtual_memory()
    cpu_percent = psutil.cpu_percent(interval=1)
    disk = psutil.disk_usage('/')

    # 进程信息
    process = psutil.Process(os.getpid())
    process_memory = process.memory_info()

    return {
        "system": {
            "cpu_percent": cpu_percent,
            "memory_used_mb": memory.used / 1024 / 1024,
            "memory_total_mb": memory.total / 1024 / 1024,
            "memory_percent": memory.percent,
            "disk_used_gb": disk.used / 1024 / 1024 / 1024,
            "disk_total_gb": disk.total / 1024 / 1024 / 1024,
            "disk_percent": disk.percent,
        },
        "process": {
            "pid": process.pid,
            "memory_rss_mb": process_memory.rss / 1024 / 1024,
            "memory_vms_mb": process_memory.vms / 1024 / 1024,
            "num_threads": process.num_threads(),
        },
    }


@router.get("/ready")
async def readiness_check():
    """就绪检查端点 - 用于 Kubernetes."""
    return {"ready": True}
```

---

## 8. 滚动更新与回滚策略

### Docker Compose 滚动更新脚本

#### `scripts/rolling-update.sh`

```bash
#!/bin/bash
set -e

echo "=== DeepResearchWeb 滚动更新 ==="

CURRENT_TAG=${1:-latest}
NEW_IMAGE="deepresearchweb/backend:$CURRENT_TAG"

echo "更新镜像: $NEW_IMAGE"

# 拉取新镜像
docker-compose -f docker-compose.prod.yml pull backend

# 滚动更新 backend 服务（零停机）
docker-compose -f docker-compose.prod.yml up -d --no-deps --build backend

# 等待新服务就绪
echo "等待服务就绪..."
sleep 10

# 健康检查
if curl -sf http://localhost/api/v1/monitoring/health > /dev/null; then
    echo "更新成功!"
else
    echo "健康检查失败，开始回滚..."
    docker-compose -f docker-compose.prod.yml rollback backend
    exit 1
fi
```

---

## 9. 备份与恢复策略

### 数据库备份脚本

#### `scripts/backup.sh`

```bash
#!/bin/bash
set -e

BACKUP_DIR="./backups"
DATE=$(date +%Y%m%d_%H%M%S)
MYSQL_CONTAINER="drw_mysql"
NEO4J_CONTAINER="drw_neo4j"

mkdir -p "$BACKUP_DIR"

# MySQL 备份
echo "备份 MySQL..."
docker exec "$MYSQL_CONTAINER" mysqldump \
    -u drw -p"$MYSQL_PASSWORD" deepresearch \
    > "$BACKUP_DIR/mysql_${DATE}.sql"

# 压缩备份
gzip "$BACKUP_DIR/mysql_${DATE}.sql"

# Neo4j 备份
echo "备份 Neo4j..."
docker exec "$NEO4J_CONTAINER" neo4j-admin database dump neo4j \
    --to-path="/backup/neo4j_${DATE}"

# Redis 备份 (RDB)
echo "备份 Redis..."
docker exec "$NEO4J_CONTAINER" redis-cli SAVE

# 清理 7 天前的备份
find "$BACKUP_DIR" -name "*.sql.gz" -mtime +7 -delete

echo "备份完成: $DATE"
```

### 数据恢复脚本

#### `scripts/restore.sh`

```bash
#!/bin/bash
set -e

if [ -z "$1" ]; then
    echo "用法: $0 <backup_date>"
    echo "可用备份:"
    ls -1 backups/*.sql.gz
    exit 1
fi

BACKUP_DATE=$1
MYSQL_CONTAINER="drw_mysql"

echo "从 $BACKUP_DATE 恢复 MySQL..."

# 停止应用
docker-compose -f docker-compose.prod.yml stop backend worker

# 恢复数据库
gunzip -c "./backups/mysql_${BACKUP_DATE}.sql" | \
    docker exec -i "$MYSQL_CONTAINER" mysql -u drw -p"$MYSQL_PASSWORD" deepresearch

# 重启应用
docker-compose -f docker-compose.prod.yml start backend worker

echo "恢复完成!"
```

---

## 10. 性能优化最佳实践

### 后端连接池优化

```python
# backend/app/db/database.py - 扩展配置

engine = create_async_engine(
    settings.DATABASE_URL.replace("mysql+pymysql", "mysql+aiomysql"),
    pool_size=settings.DB_POOL_SIZE,
    max_overflow=settings.DB_MAX_OVERFLOW,
    pool_recycle=settings.DB_POOL_RECYCLE,
    pool_pre_ping=True,  # 连接前测试
    pool_timeout=30,     # 获取连接超时
    echo=settings.DEBUG,
)

# 推荐配置
# DB_POOL_SIZE = 20 (CPU 核心数 * 2 + 磁盘数)
# DB_MAX_OVERFLOW = 30
# DB_POOL_RECYCLE = 3600 (1小时)
```

### 缓存策略

```python
# backend/app/core/cache.py - 扩展缓存策略

class CacheStrategy:
    """缓存策略配置"""

    # 短 TTL - 频繁变化的数据
    SHORT_TTL = 300    # 5分钟

    # 中 TTL - 一般数据
    MEDIUM_TTL = 1800  # 30分钟

    # 长 TTL - 静态数据
    LONG_TTL = 3600    # 1小时

    # 搜索结果缓存
    SEARCH_TTL = 1800  # 30分钟

    # LLM 响应缓存 (谨慎使用)
    LLM_RESPONSE_TTL = 7200  # 2小时
```

### 前端构建优化

```typescript
// frontend/vite.config.ts - 优化配置

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    cssCodeSplit: true,
    sourcemap: false,
    chunkSizeWarningLimit: 1000,
    rollupOptions: {
      output: {
        manualChunks:-react': ['react {
          'vendor', 'react-dom', 'react-router-dom'],
          'vendor-query': ['@tanstack/react-query'],
          'vendor-ui': ['lucide-react'],
        },
      },
    },
    minify: 'terser',
  },
  optimizeDeps: {
    include: ['react', 'react-dom', 'react-router-dom', '@tanstack/react-query'],
  },
});
```

---

## 11. 安全加固

### Nginx 安全配置增强

```nginx
# nginx/security.conf

# 防 DDoS - 限制请求速率
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=3r/s;

# 防爬虫
if ($http_user_agent ~* (Scrapy|crawl|spider|bot)) {
    return 403;
}

# 隐藏 Nginx 版本
server_tokens off;

# 限制 HTTP 方法
if ($request_method !~ ^(GET|POST|OPTIONS)$) {
    return 405;
}

# 添加安全 Cookie 属性
proxy_cookie_path / "/; Secure; HttpOnly; SameSite=Strict";
```

### API 限流配置

```python
# backend/app/core/rate_limit.py

from fastapi import Request, HTTPException
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

# 认证限流
@router.post("/auth/login")
@limiter.limit("3/minute")
async def login(request: Request):
    ...

# 搜索限流
@router.post("/research/search")
@limiter.limit("10/minute")
async def search(request: Request):
    ...

# 聊天限流
@router.post("/chat/message")
@limiter.limit("20/minute")
async def chat(request: Request):
    ...
```

---

## 12. 灾难恢复计划

### RTO 和 RPO 目标

| 组件 | RTO | RPO | 备份频率 |
|------|-----|-----|----------|
| MySQL | 30 分钟 | 1 小时 | 每日 + 增量 |
| Neo4j | 1 小时 | 24 小时 | 每日 |
| Redis | 5 分钟 | N/A (AOF) | 实时 |
| 应用代码 | 15 分钟 | N/A | CI/CD |
| 用户上传 | 1 小时 | 24 小时 | 每日 |

### 灾难恢复流程

1. **检测阶段**: 监控系统告警 → 确认故障
2. **决策阶段**: 评估影响 → 决定是否启动 DR
3. **恢复阶段**:
   - 切换到备份数据库
   - 启动备用服务
   - 更新 DNS/负载均衡
4. **验证阶段**: 健康检查 → 确认服务可用
5. **通知阶段**: 通知用户 → 记录事件
