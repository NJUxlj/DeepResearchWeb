# 05. ARQ 任务队列与并发控制

## 目标

实现异步任务队列和并发控制：
- ARQ 异步任务队列集成
- Redis Pub/Sub SSE 消息推送
- LLM 并发控制（信号量）
- 搜索结果缓存
- MCP 进程池管理

## 依赖

- `01.scaffold.md`: Redis 连接、Docker 环境
- `04.research-agent-core.md`: Research Agent

## 被依赖

- `06.memos-integration.md`: 记忆写入异步任务
- `08.frontend-layout-citation.md`: SSE 消息推送

---

## 1. Redis 连接增强

### `backend/app/core/redis.py`

```python
"""Redis 连接管理和工具函数."""

import json
from typing import Any

import redis.asyncio as redis

from app.config import settings

# Redis 连接池
_redis_pool: redis.Redis | None = None


async def get_redis() -> redis.Redis:
    """获取 Redis 连接（单例模式）."""
    global _redis_pool
    if _redis_pool is None:
        _redis_pool = redis.from_url(
            settings.REDIS_URL,
            max_connections=settings.REDIS_MAX_CONNECTIONS,
            decode_responses=True,
        )
    return _redis_pool


async def close_redis() -> None:
    """关闭 Redis 连接."""
    global _redis_pool
    if _redis_pool:
        await _redis_pool.close()
        _redis_pool = None


# ========== Pub/Sub 功能 ==========

async def publish_message(channel: str, message: dict[str, Any]) -> None:
    """发布消息到指定频道."""
    r = await get_redis()
    await r.publish(channel, json.dumps(message))


async def subscribe_channel(channel: str):
    """订阅指定频道，返回 pubsub 对象."""
    r = await get_redis()
    pubsub = r.pubsub()
    await pubsub.subscribe(channel)
    return pubsub


# ========== 缓存功能 ==========

async def cache_get(key: str) -> Any:
    """获取缓存."""
    r = await get_redis()
    value = await r.get(key)
    if value:
        return json.loads(value)
    return None


async def cache_set(key: str, value: Any, ttl: int = 1800) -> None:
    """设置缓存（默认 30 分钟）."""
    r = await get_redis()
    await r.setex(key, ttl, json.dumps(value))


async def cache_delete(key: str) -> None:
    """删除缓存."""
    r = await get_redis()
    await r.delete(key)


def generate_cache_key(prefix: str, *args) -> str:
    """生成缓存键."""
    import hashlib
    content = ":".join(str(arg) for arg in args)
    hash_value = hashlib.md5(content.encode()).hexdigest()
    return f"{prefix}:{hash_value}"
```

---

## 2. LLM 并发控制

### `backend/app/core/rate_limiter.py`

```python
"""LLM 并发控制和速率限制."""

import asyncio
from datetime import datetime, timedelta
from typing import Any, Callable, TypeVar

from app.config import settings
from app.core.redis import cache_get, cache_set, get_redis

T = TypeVar("T")


class LLMRateLimiter:
    """LLM 调用速率限制器."""

    def __init__(self):
        self._semaphore = asyncio.Semaphore(settings.LLM_MAX_CONCURRENT)
        self._rpm_limit = 500  # 每分钟请求数限制
        self._window_size = 60  # 滑动窗口大小（秒）

    async def call(self, func: Callable[..., T], *args, **kwargs) -> T:
        """
        在信号量控制下执行函数.
        
        Args:
            func: 要执行的异步函数
            *args, **kwargs: 函数参数
        
        Returns:
            函数执行结果
        """
        # 检查 RPM 限制
        await self._check_rpm_limit()
        
        async with self._semaphore:
            return await func(*args, **kwargs)

    async def _check_rpm_limit(self) -> None:
        """检查并等待 RPM 限制."""
        r = await get_redis()
        key = "llm:rpm:count"
        
        current = await r.get(key)
        if current and int(current) >= self._rpm_limit:
            # 等待窗口重置
            await asyncio.sleep(1)
            await self._check_rpm_limit()
        else:
            # 增加计数
            pipe = r.pipeline()
            pipe.incr(key)
            pipe.expire(key, self._window_size)
            await pipe.execute()


# 全局限流器实例
llm_limiter = LLMRateLimiter()


async def limited_llm_call(func: Callable[..., T], *args, **kwargs) -> T:
    """使用全局限流器调用 LLM."""
    return await llm_limiter.call(func, *args, **kwargs)
```

---

## 3. ARQ Worker 配置

### `backend/app/workers/research_worker.py`

```python
"""ARQ Worker: 异步执行研究任务."""

import asyncio
from datetime import datetime
from typing import Any

from arq import create_pool
from arq.connections import RedisSettings

from app.agents.research_agent import ResearchAgent
from app.config import settings
from app.core.redis import publish_message
from app.schemas.research import ResearchConfig

# ARQ Redis 配置
arq_redis_settings = RedisSettings.from_dsn(settings.REDIS_URL)


async def run_research_task(
    ctx: dict,
    task_id: int,
    query: str,
    user_id: int,
    session_id: int,
    config: dict[str, Any],
) -> dict[str, Any]:
    """
    执行研究任务.
    
    这是一个 ARQ 任务，在 Worker 进程中异步执行.
    """
    import asyncmy
    from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
    from sqlalchemy.orm import sessionmaker
    
    # 创建数据库连接
    engine = create_async_engine(
        settings.DATABASE_URL.replace("mysql+pymysql", "mysql+aiomysql"),
        pool_size=5,
        max_overflow=10,
    )
    async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
    
    try:
        async with async_session() as db:
            # 更新任务状态为 running
            from app.models.research_task import ResearchTask
            
            result = await db.execute(
                select(ResearchTask).where(ResearchTask.id == task_id)
            )
            task = result.scalar_one_or_none()
            
            if task:
                task.status = "running"
                task.started_at = datetime.utcnow()
                await db.commit()
            
            # 执行研究
            research_config = ResearchConfig(**config)
            agent = ResearchAgent(research_config)
            
            async def on_event(event: str, data: dict):
                """事件回调，通过 Redis Pub/Sub 推送."""
                await publish_message(
                    f"research:{task_id}",
                    {"event": event, "data": data, "timestamp": datetime.utcnow().isoformat()}
                )
            
            result = await agent.research(query, user_id, on_event)
            
            # 更新任务完成状态
            if task:
                task.status = "completed"
                task.completed_at = datetime.utcnow()
                task.iterations = len(result.iterations)
                task.sources_count = result.total_sources
                task.result = {
                    "query": result.query,
                    "final_report": result.final_report,
                    "citations": [c.model_dump() for c in result.citations],
                    "total_sources": result.total_sources,
                    "completed_at": result.completed_at.isoformat(),
                }
                await db.commit()
            
            # 发送完成消息
            await publish_message(
                f"research:{task_id}",
                {"event": "complete", "data": {"task_id": task_id}}
            )
            
            return {"status": "completed", "task_id": task_id}
    
    except Exception as e:
        # 更新失败状态
        async with async_session() as db:
            from app.models.research_task import ResearchTask
            from sqlalchemy import select
            
            result = await db.execute(
                select(ResearchTask).where(ResearchTask.id == task_id)
            )
            task = result.scalar_one_or_none()
            
            if task:
                task.status = "failed"
                task.error = str(e)
                await db.commit()
        
        # 发送错误消息
        await publish_message(
            f"research:{task_id}",
            {"event": "error", "data": {"error": str(e)}}
        )
        
        raise
    
    finally:
        await engine.dispose()


class WorkerSettings:
    """ARQ Worker 配置."""

    redis_settings = arq_redis_settings
    
    # 任务函数
    functions = [run_research_task]
    
    # 并发设置
    max_jobs = 50  # 最大并发任务数
    job_timeout = 600  # 任务超时时间（秒）
    
    # 重试设置
    retry_jobs = True
    max_tries = 3
    
    # 健康检查
    health_check_interval = 30


async def enqueue_research_task(
    redis,
    task_id: int,
    query: str,
    user_id: int,
    session_id: int,
    config: dict[str, Any],
) -> str:
    """将研究任务加入队列."""
    job = await redis.enqueue_job(
        "run_research_task",
        task_id=task_id,
        query=query,
        user_id=user_id,
        session_id=session_id,
        config=config,
    )
    return job.job_id
```

---

## 4. SSE 与 Redis Pub/Sub 集成

### `backend/app/api/v1/research.py` 更新（使用 ARQ）

```python
"""Research API 端点（ARQ 集成版本）."""

import asyncio
import json
from datetime import datetime

from fastapi import APIRouter, HTTPException
from fastapi.responses import StreamingResponse
from sqlalchemy import select

from app.api.deps import CurrentUserDep, DBDep
from app.core.redis import get_redis, subscribe_channel
from app.models.research_task import ResearchTask
from app.models.session import Session
from app.schemas.research import ResearchConfig, ResearchTaskDetail
from app.workers.research_worker import enqueue_research_task

router = APIRouter(prefix="/research", tags=["research"])


@router.post("/tasks")
async def create_research_task(
    db: DBDep,
    current_user: CurrentUserDep,
    query: str,
    session_id: int | None = None,
    config: ResearchConfig | None = None,
):
    """
    创建研究任务并加入队列.
    """
    if not query:
        raise HTTPException(status_code=422, detail="Query is required")

    # 获取或创建会话
    if session_id:
        result = await db.execute(
            select(Session).where(
                Session.id == session_id,
                Session.user_id == current_user.id,
            )
        )
        session = result.scalar_one_or_none()
        if session is None:
            raise HTTPException(status_code=404, detail="Session not found")
    else:
        session = Session(
            user_id=current_user.id,
            title=query[:50] + "..." if len(query) > 50 else query,
            mode="research",
        )
        db.add(session)
        await db.commit()
        await db.refresh(session)

    # 创建任务记录
    task = ResearchTask(
        session_id=session.id,
        user_id=current_user.id,
        query=query,
        config=config.model_dump() if config else {},
        status="pending",
    )
    db.add(task)
    await db.commit()
    await db.refresh(task)

    # 加入 ARQ 队列
    redis = await get_redis()
    job_id = await enqueue_research_task(
        redis,
        task_id=task.id,
        query=query,
        user_id=current_user.id,
        session_id=session.id,
        config=config.model_dump() if config else {},
    )

    return {
        "task_id": task.id,
        "job_id": job_id,
        "status": "pending",
        "session_id": session.id,
    }


@router.get("/tasks/{task_id}/stream")
async def research_task_stream(
    db: DBDep,
    current_user: CurrentUserDep,
    task_id: int,
):
    """
    通过 SSE 获取研究任务实时进度.
    
    使用 Redis Pub/Sub 接收 Worker 推送的消息.
    """
    # 验证任务归属
    result = await db.execute(
        select(ResearchTask).where(
            ResearchTask.id == task_id,
            ResearchTask.user_id == current_user.id,
        )
    )
    task = result.scalar_one_or_none()
    
    if task is None:
        raise HTTPException(status_code=404, detail="Task not found")

    channel = f"research:{task_id}"

    async def event_generator():
        """生成 SSE 事件流."""
        pubsub = await subscribe_channel(channel)
        
        try:
            # 发送初始状态
            yield f"event: status\ndata: {json.dumps({'status': task.status})}\n\n"
            
            # 监听 Redis 消息
            async for message in pubsub.listen():
                if message["type"] == "message":
                    data = json.loads(message["data"])
                    event_type = data.get("event", "message")
                    
                    yield f"event: {event_type}\ndata: {json.dumps(data)}\n\n"
                    
                    # 任务完成或出错时结束
                    if event_type in ("complete", "error"):
                        break
        
        finally:
            await pubsub.unsubscribe(channel)
            await pubsub.close()

    return StreamingResponse(
        event_generator(),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Task-Id": str(task_id),
        },
    )
```

---

## 5. 搜索结果缓存

### `backend/app/core/cache.py`

```python
"""搜索结果缓存管理."""

import hashlib
from typing import Any

from app.core.redis import cache_get, cache_set, generate_cache_key


class SearchCache:
    """搜索缓存管理器."""

    DEFAULT_TTL = 1800  # 30 分钟

    @staticmethod
    def generate_key(query: str, **kwargs) -> str:
        """生成搜索缓存键."""
        params = f"{query}:{sorted(kwargs.items())}"
        return generate_cache_key("search", params)

    @staticmethod
    async def get(query: str, **kwargs) -> Any:
        """获取缓存的搜索结果."""
        key = SearchCache.generate_key(query, **kwargs)
        return await cache_get(key)

    @staticmethod
    async def set(query: str, results: Any, ttl: int = DEFAULT_TTL, **kwargs) -> None:
        """缓存搜索结果."""
        key = SearchCache.generate_key(query, **kwargs)
        await cache_set(key, results, ttl)


async def cached_search(search_func, query: str, **kwargs):
    """
    带缓存的搜索装饰器.
    
    Usage:
        results = await cached_search(tavily_search, "query", max_results=5)
    """
    # 尝试从缓存获取
    cached = await SearchCache.get(query, **kwargs)
    if cached is not None:
        return cached

    # 执行搜索
    results = await search_func(query, **kwargs)
    
    # 缓存结果
    await SearchCache.set(query, results, **kwargs)
    
    return results
```

---

## 6. MCP 进程池管理

### `backend/app/services/mcp_service.py`

```python
"""MCP 服务进程池管理."""

import asyncio
from datetime import datetime, timedelta
from typing import Any

from app.config import settings


class MCPProcessPool:
    """MCP Server 进程池管理器."""

    def __init__(self, max_processes: int = 20, idle_timeout: int = 300):
        self.max_processes = max_processes
        self.idle_timeout = idle_timeout  # 5 分钟
        self._processes: dict[str, Any] = {}  # server_id -> process
        self._last_used: dict[str, datetime] = {}
        self._semaphore = asyncio.Semaphore(max_processes)
        self._lock = asyncio.Lock()

    async def get_process(self, server_id: str, server_config: dict) -> Any:
        """
        获取或创建 MCP Server 进程.
        
        Args:
            server_id: 服务器唯一标识
            server_config: 服务器配置
        
        Returns:
            MCP 进程会话
        """
        async with self._lock:
            # 检查是否有现有进程
            if server_id in self._processes:
                self._last_used[server_id] = datetime.utcnow()
                return self._processes[server_id]

        # 等待信号量
        async with self._semaphore:
            async with self._lock:
                # 双重检查
                if server_id in self._processes:
                    self._last_used[server_id] = datetime.utcnow()
                    return self._processes[server_id]

                # 创建新进程
                process = await self._create_process(server_config)
                self._processes[server_id] = process
                self._last_used[server_id] = datetime.utcnow()
                
                # 启动空闲检查
                asyncio.create_task(self._idle_check(server_id))
                
                return process

    async def _create_process(self, server_config: dict) -> Any:
        """创建 MCP Server 进程."""
        from mcp import ClientSession, StdioServerParameters
        from mcp.client.stdio import stdio_client

        params = StdioServerParameters(
            command=server_config["command"],
            args=server_config.get("args", []),
            env=server_config.get("env"),
        )

        # 创建客户端
        read, write = await stdio_client(params).__aenter__()
        session = await ClientSession(read, write).__aenter__()
        await session.initialize()
        
        return {"session": session, "read": read, "write": write}

    async def _idle_check(self, server_id: str) -> None:
        """检查进程空闲状态，超时后关闭."""
        while True:
            await asyncio.sleep(60)  # 每分钟检查一次
            
            async with self._lock:
                if server_id not in self._last_used:
                    return
                
                last_used = self._last_used[server_id]
                if datetime.utcnow() - last_used > timedelta(seconds=self.idle_timeout):
                    # 关闭进程
                    await self._close_process(server_id)
                    return

    async def _close_process(self, server_id: str) -> None:
        """关闭指定进程."""
        if server_id in self._processes:
            process = self._processes.pop(server_id)
            self._last_used.pop(server_id, None)
            
            try:
                await process["session"].__aexit__(None, None, None)
                await process["write"].__aexit__(None, None, None)
            except Exception as e:
                print(f"Error closing MCP process {server_id}: {e}")

    async def call_tool(
        self,
        server_id: str,
        tool_name: str,
        arguments: dict,
    ) -> Any:
        """调用 MCP 工具."""
        # 这里需要获取 server_config，简化实现
        # 实际实现中需要从数据库查询配置
        process = await self.get_process(server_id, {})
        
        result = await process["session"].call_tool(tool_name, arguments)
        
        # 更新最后使用时间
        async with self._lock:
            self._last_used[server_id] = datetime.utcnow()
        
        return result

    async def close_all(self) -> None:
        """关闭所有进程."""
        async with self._lock:
            for server_id in list(self._processes.keys()):
                await self._close_process(server_id)


# 全局 MCP 进程池
mcp_pool = MCPProcessPool(
    max_processes=20,
    idle_timeout=300,
)
```

---

## 7. 更新 requirements.txt

```txt
# ARQ 任务队列
arq>=0.25.0

# Tavily 搜索（已在 04 中添加）
tavily-python>=0.3.0
```

---

## 接口契约

### 并发参数

| 组件 | 配置 | 说明 |
|------|------|------|
| ARQ Workers | max_jobs=50 | 最大并发研究任务 |
| LLM 并发 | semaphore=20 | 同时 LLM 调用数 |
| MCP 进程池 | max_processes=20 | 最大 MCP Server 进程 |
| 缓存 TTL | 1800s | 搜索结果缓存时间 |

### Redis 键命名规范

```
search:<hash>          # 搜索结果缓存
research:<task_id>     # 研究任务 Pub/Sub 频道
llm:rpm:count          # LLM 每分钟请求计数
mcp:process:<id>       # MCP 进程信息
```

---

## 验证清单

- [ ] ARQ Worker 正常启动并消费任务
- [ ] Redis Pub/Sub 消息正确推送
- [ ] SSE 流正确接收事件
- [ ] LLM 并发限制生效
- [ ] 搜索结果缓存命中
- [ ] MCP 进程池管理正常
